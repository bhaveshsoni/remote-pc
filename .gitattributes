# Normalize line endings: text files use LF in the repository.
* text=auto eol=lf

# Windows batch scripts must stay CRLF to run correctly.
*.bat text eol=crlf

# Treat known binary types as binary (no EOL conversion / diffs).
*.png binary
*.jpg binary
*.ico binary
*.p12 binary
*.jks binary
*.jar binary
